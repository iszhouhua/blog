spring:
  thymeleaf:
    cache: true
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://host.docker.internal:3306/blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8&useSSL=false&autoReconnect=true
    username: root
    password: 123456
  mail:
    host: smtp.iszhouhua.com
    username: test@iszhouhua.com
    password: Zh123456
  # flyway配置
  flyway:
    # 禁止清理数据表
    clean-disabled: true